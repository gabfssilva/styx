apply plugin: 'scala'

repositories {
    jcenter()
    mavenLocal()
}

subprojects {
    apply plugin: 'scala'

    repositories {
        jcenter()
        mavenLocal()
    }

    dependencies {
        compile project(':core')
    }
}